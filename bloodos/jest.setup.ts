
// Import jest-dom to automatically add custom matchers for asserting on DOM nodes.
// Allows using expect(element).toBeInTheDocument() or expecting element attributes
import '@testing-library/jest-dom';
